import "./buildMap.css"


function buildMap() {
    const width = window.innerWidth;
    const height = window.innerHeight;
    console.log(width, height);
}

const tiles = `
    <div class="woodBlock"></div>
    <div class="stoneBlock"></div>
`
export default tiles

